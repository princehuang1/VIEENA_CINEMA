{
  "name": "project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "frontend": "cd movie-frontend && npm run dev",
    "backend": "cd movie-backend && node index.js",
    "dev": "concurrently \"npm run frontend\" \"npm run backend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
